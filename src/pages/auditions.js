import React, { Component } from "react"
import Layout from "../components/layout"
import PageTitle from "../components/pageTitle"

export class auditions extends Component {
  render() {
    return (
      <Layout>
        <PageTitle />
      </Layout>
    )
  }
}

export default auditions
